# Supabase Configuration
# Get these values from your Supabase project dashboard:
# https://app.supabase.com/ → Settings → API

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key-here

# Note: These values should be added to src/environments/environment.ts
# for local development and src/environments/environment.prod.ts for production
# The anon key is safe to expose as RLS policies enforce read-only access
